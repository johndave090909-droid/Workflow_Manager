rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Any signed-in user can read/write storage
    // Tighten these rules before deploying to production
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
