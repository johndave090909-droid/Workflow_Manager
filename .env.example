# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# GITHUB_TOKEN: Required to trigger screenshot workflow from the site.
# Create at github.com → Settings → Developer settings → Personal access tokens → Tokens (classic)
# Required scope: workflow
GITHUB_TOKEN="ghp_your_token_here"

# FB_PAGE_TOKEN: Required for the Facebook node to send messages via Messenger.
# Get from: Meta for Developers → Your App → Messenger → Page Access Token
# Required permission: pages_messaging
# The recipient must have previously sent a message to your page (24h window).
FB_PAGE_TOKEN="your_page_access_token_here"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# ── Email (SMTP) ──────────────────────────────────────────────────────────────
# Required for the Email node to send messages.
# For Gmail: use an App Password (not your main password).
#   myaccount.google.com → Security → 2-Step Verification → App passwords
# For other providers: use the provider's SMTP settings.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your_email@gmail.com"
SMTP_PASS="your_app_password_here"
SMTP_FROM="Workflow Manager <your_email@gmail.com>"

# FIREBASE_SERVICE_ACCOUNT: Required for IT admin to change user email/password.
# Get from: Firebase Console → Project Settings → Service Accounts → Generate new private key
# Paste the entire JSON as a single-line string (stringify it).
FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'
